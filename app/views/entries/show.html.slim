- @page_title = @entry.title + "-" + @entry.member.name + "さんのブログ"
h1 #{@entry.member.name}さんのブログ
h2 = @entry.title

= the_first_image(@entry)
= simple_format(@entry.body)
= other_images(@entry)
= render 'footer', entry: @entry
